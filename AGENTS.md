# AGENTS.md

## 作業開始時

- 最初に `HANDOFF.md` と本ファイルを読み、作業開始前に `git status --short --branch` と `git rev-parse HEAD` / `git rev-parse origin/main` を確認する。
- Gitと実際の `index.html`・画像の状態を正とし、文書と食い違う場合は実態を確認してからHANDOFFを更新する。
- 既存の未コミット変更や未追跡ファイルを、明示依頼なしに削除・上書き・巻き戻ししない。特に `.DS_Store` など作業対象外の変更はコミットに含めない。

## プロジェクトの前提

- このプロジェクトはビルドシステムを持たない、画像ファイルと単一の `index.html` で構成された静的サイトである。
- サイトの目的は、サポートセンターわかくさへの利用相談・見学・関係機関からの連絡と、正社員・パート・ボランティアの採用応募につなげること。
- 連絡先・住所・サービス内容・求人条件は、確認できた事実だけを掲載する。未確認の給与額、対応範囲、制度、実績を推測して追加しない。
- 既存の画像素材は、内容と文脈が合う場所で再利用する。画像を差し替える場合は、代替テキストと読み込み負荷も確認する。

## 変更・安全境界

- `index.html` の文言・導線・レスポンシブUIを変更する際は、利用相談と求人の両方の主要導線、電話番号、メールアドレス、住所、フォーム送信先を壊さない。
- 外部フォーム（Formspree）へ実データを送信しない。フォーム送信先や公開ドメインを変更する場合は、ユーザーの明示承認を得る。
- `.env*`、トークン、APIキー、秘密鍵、認証情報は表示・記録・コミットしない。このリポジトリに秘密情報を追加しない。
- commit、push、deploy、公開ページへの外部書き込みは、ユーザーの明示許可がある場合だけ行う。公開前に対象リモートと本番URLを確認する。

## 検証

- 変更後は `git diff --check` を実行する。
- 静的チェックでは、内部アンカーの存在、ローカル画像参照、画像の `alt`、インラインJavaScript、JSON-LDの構文を確認する。
- 必要に応じてローカル確認は `python3 -m http.server 8000` で行う。ブラウザではスマホ幅・PC幅、主要CTA、固定メニュー、フォーム表示を確認するが、フォーム送信は行わない。
- 現在の公開先はVercelで、GitHubの `main` へのpushが本番反映の契機である。反映後は `https://wakakusa-care.com/` と `https://wakakusa-care.vercel.app/` のレスポンスと新しい内容を確認する。
- 実行した検証、失敗、未実施の重要な確認は、作業終了時に `HANDOFF.md` と最終報告へ記録する。

## HANDOFFの管理

- 作業開始時は `HANDOFF.md` の現行スナップショットを読み、必要な範囲だけ関連資料を読む。
- `HANDOFF.md` は履歴台帳ではなく、現在の目的・Git状態・変更範囲・検証結果・次の作業・判断待ちだけを40〜80行程度で保つ。
- 詳細な完了履歴や古いコマンドログを残す必要が生じた場合だけ `docs/handoffs/YYYY-MM-DD-<topic>.md` にアーカイブし、通常のHANDOFFには要約とリンクだけを書く。
- `HANDOFF.md` は確認済みの事実だけで更新し、未来のコミットSHAや未確認のデプロイ成功を記載しない。
