# HANDOFF

## 更新情報

- 更新日時: 2026-09-13 Asia/Tokyo
- 更新者: Claude Code
- 現在の目的: デザインの体系整理（文字サイズ・角丸）を終え、残る確認事項を事業所に聞く。

## 現在の状態

- ブランチ: `main`
- HEAD / upstream: `main` は `origin/main` と一致（`5e71c51`）。
- 作業ツリー: **未コミットの変更あり**。`index.html`（トークン化と200%表示の修正）、
  `HANDOFF.md`、`docs/handoffs/2026-09-11-review-and-illustrations.md`（新規）。
  本番へは未反映。
- GitHub: `https://github.com/sanrenikuma/wakakusa-care`
- 公開先: Vercel。`https://wakakusa-care.com/` と `https://wakakusa-care.vercel.app/`
- 構成: `index.html`、画像（JPEG / WebP）、`thanks.html`、`favicon.svg`、`robots.txt`、`sitemap.xml`。
  フォーム送信先は従来の Formspree のまま。

## 直近で完了したこと（未コミット）

- **文字サイズをトークン化した。** 素の値28種・85宣言を、本文6段（`--fs-xs`〜`--fs-xl`）と
  見出し5段（`--fs-2xl`〜`--fs-6xl`）へ集約。幅で伸縮する見出しの `clamp()` 6種と、
  入力欄の `16px`（iOSの自動拡大を防ぐため）は据え置き。段の値は使用回数で重み付けして選び、
  最大ずれ0.80px（5%）に収めた。
- **集約で無効になった上書き4件を削除した。** `.topbar-inner` `.hero-sticker` `.benefit-pill`
  `.stat span` のモバイル用 `font-size`。基底ルールと同じ値になり、効果がなくなったもの。
- **角丸をトークン化した。** `--radius-xs`(10) `-sm`(12) `-md`(16) `-lg`(18) `-xl`(24)
  `-2xl`(28) `-pill`(999) の7段。既存の `--radius-md`(18) / `--radius-lg`(28) は段順が
  崩れるため改名した（`-lg` / `-2xl`）。640px以下で20pxへ下がるのは `--radius-2xl` のみ。
- **box-shadow は変更していない。** 12種のうち複数回使われるものは既に `--shadow-sm`（5回）
  `--shadow-lg`（2回）としてトークン化済みで、残る9種はすべて1回ずつの使用。まとめると
  重複が減るのではなく見た目が変わるため、判断を残した（下記「判断待ち」）。
- **文字サイズ200%での横溢れを直した。** `.volunteer-row .btn-text` の `white-space: nowrap` が
  原因。同ファイル内の `.footer-cta` と同じ対処（640px以下で `normal`）を当てた。

## 検証済み

- 要素ごとの機械比較 — 成功。2026-09-13。変更前後で全393要素の `font-size` / `border-radius` /
  `box-shadow` / 実寸を突き合わせた。1280px・390px・390pxの文字200%の3条件すべてで、
  文字サイズのずれは最大0.80px、角丸の変化は意図した5要素の±1pxのみ
  （`.stat` 15→16、地図iframe 19→18、入力欄3種 11→12）、影の変化は0件。
- 文字サイズ200%での横溢れ — **失敗 → 修正済み**。2026-09-13。390px幅で変更前は
  `scrollWidth 414`（`clientWidth 390`）。トークン化で一時422まで広がったが、原因を直して
  390（溢れ0）になった。**この溢れは今回の変更前から存在していた。**
- 静的チェック — 成功。2026-09-13。`id` 重複なし（22個一意）、内部アンカー9件全解決、
  ローカル画像16件実在、`img` 全8枚に `alt` と `width`/`height`、`innerHTML`/`eval` 0件、
  JSON-LD構文正常、秘密情報なし、CSSの波括弧が釣り合う、未定義・未使用のCSS変数0件。
- 実描画確認 — 成功。2026-09-13。1280px・390px・281px、および文字200%で確認。
- 281px幅の横溢れ — **既存の問題。今回の変更が原因ではない。** 等倍は変更前後とも
  `scrollWidth 320`（ヘッダーとtopbarが320pxで下げ止まる）。文字200%では変更前414が333へ改善した。
  281pxは実在する端末より狭いため、優先度は低い。
- 未実施: 実機iPhone、実機でのキーボード操作、フォーム実送信、本番反映後の確認。

## 検証方法の注意

- **スマホ表示の確認に `chrome --headless --window-size=390,...` を使ってはいけない。**
  macOSのChromeはウィンドウ幅に下限（約500px）があり、390pxを指定しても実際は約485〜500pxで組まれる。
  デバイスエミュレーションを使う。
- **ローカルサーバーはキャッシュを返す。** 再読み込みだけでは古いCSSのままになり、検証が空振りする。
  クエリ文字列を変える（`?v=2`）などでキャッシュを外してから測る。
- 文字サイズ200%は、ルート要素の `font-size` を32pxにすると `rem` 基準が倍になり等価に再現できる。
- iframe方式はモバイルブラウザ特有のビューポート挙動を再現しないため、実機確認は別途必要。

## 次に行うこと

1. 今回の変更をコミットするか判断する（3つに分けられる：文字サイズ / 角丸 / 200%溢れの修正）。
2. 残る確認事項を事業所に聞く（下記2件）。
3. 実機iPhoneでの確認。特に、横180pxのはみ出しに起因していた固定バーの症状が解消しているか。

## 判断待ち・禁止事項

- 判断待ち（従来分）: 求人の給与・勤務条件、Formspree完了後の運用。
- 判断待ち（今回）: box-shadow の3つ（`.btn-primary` `0 10px 22px/.18`、`.form-submit`
  `0 9px 20px/.22`、`.brand-mark` `0 8px 18px/.2`）は、色が違うだけでほぼ同じ「浮き」の影。
  1つに揃えれば12種→10種になるが、1〜2pxの見た目の変化を伴う。
- 確認待ち（残り2件）:
  - 事業所番号（障害福祉サービス事業者番号）。相談支援専門員が事業所を照合するのに使う。
    サービス名は確認済み（重度訪問介護・居宅介護・行動援護・移動支援、
    および介護保険の訪問介護・介護予防訪問介護）。
  - 実際によくある相談の例。`#support` にはA向けの例が1件しかなく、しかも抽象的。
    利用者の言葉に近い形で3〜4件並べると、読んだ家族が相談に踏み切りやすくなる。
- 保留: ヒーローカードは文字サイズ200%時にカード内スクロールになる。内容は失われないが、
  写真の下へ流す構成に変える案もある。見た目の判断を伴うため未着手。
- 保留: 1061px未満ではナビ・電話・CTAがヘッダー1段に収まらないため、電話は隠して
  sticky なCTA（`#contact`）に任せている。この帯で電話を常時見せたい場合は、
  ナビ項目を減らすかCTAの文言を短くする必要がある。
- 判断済み: topbar の右枠には、重複していた電話番号の代わりに提供サービス名5つを置いた。
- 確認済み: 対象は児童から成人まで。対応エリアは大阪市全域。日中一時支援は提供していない。
  介護保険の訪問介護・介護予防訪問介護も行っている（障がい福祉がメイン）。
  返信目安は2〜3日、自動返信メールは検討中のため記載しない。
  応募は面接1回のみ15分程度、履歴書が必要。実写は出さない。
- 判断済み（事業所の方針）: 苦情受付窓口と個人情報の取扱いはサイトに載せない。
  なおフォームは要配慮個人情報に触れうる内容を受け取り、同意チェックもあるため、
  取扱いの説明がないと「何に同意したのか」が問われる形にはなる。数行で追加できる。
- 挿絵の作り直し方針: 画風はhero・移動支援・パートを基準にする。枠と同じ比率で生成すれば
  トリミングが出ない（サービス1:1 / 採用ヒーロー1.2:1 / 求人カード3:2）。
  サービスカードは左上に番号バッジ、採用ヒーローは右下にCTAと右上86pxの丸みが重なるため、
  その範囲に人物の顔や主役を置かない。原寸で良く見えても281pxで読めないことがあるので、
  実表示サイズでの確認を必ず行う。
- 禁止事項: 明示承認なしに commit、push、deploy、フォーム送信、送信先や公開ドメインの変更を行わない。
- 禁止事項: 秘密情報をリポジトリやこのファイルへ書かない。`.DS_Store` は自動追加しない。

## 関連資料

- 恒久ルール: `AGENTS.md`
- レビュー詳細（全指摘・優先度・着手順）: `docs/reviews/2026-09-10-site-review.md`
- 完了履歴（レビュー対応と挿絵の作り直し、2026-09-11）: `docs/handoffs/2026-09-11-review-and-illustrations.md`
- 設計文書: なし
