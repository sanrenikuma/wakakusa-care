# HANDOFF

## 更新情報

- 更新日時: 2026-09-11 Asia/Tokyo
- 更新者: Claude Code
- 現在の目的: 事業所への確認事項を進め、返ってきた事実を反映する。挿絵の作り直しは完了。

## 現在の状態

- ブランチ: `main`
- HEAD / upstream: `main` は `1c2b000`。挿絵6枚分は本番反映済み。
- 作業ツリー: クリーン。
- 追加ファイル: `robots.txt`、`sitemap.xml`、`recruit-job.jpg` / `.webp`（正社員カード用）。
- GitHub: `https://github.com/sanrenikuma/wakakusa-care`
- 公開先: Vercel。`https://wakakusa-care.com/` と `https://wakakusa-care.vercel.app/`
- 構成: `index.html`、画像（JPEG / WebP）、`thanks.html`、`favicon.svg`。フォーム送信先は従来の Formspree のまま。

## 直近で完了したこと

- サブエージェント5体（PC幅ビジュアル / モバイルUX / アクセシビリティ / 情報設計 / 実装品質・SEO）で `index.html` をレビューし、
  影響の大きい指摘は実ファイルと実測で裏取りした。全指摘は下記の詳細レポートに記載。
- レビュー指摘のうち、新しい事実を必要としないものを4回に分けて修正し、本番へ反映した。
  - `e449c09` 公開中の表示崩れとアクセシビリティ（横180pxのはみ出し、採用画像の引き伸ばし、
    詳細度の衝突3か所、送信後の再送信不能、フォーカスリング、入力欄の枠線、文字サイズ200%の破綻）
  - `7129480` 相談と採用の導線の穴（`#flow` のCTA、下部バーの職種強制選択、見学の入口、
    `#about` の無リンク、メニューの背面スクロール・フォーカス・状態残り）
  - `1992060` 死にコード整理と検索面（未使用フォントウェイト、`decoding`、OGP、robots/sitemap、JSON-LD拡充）
  - `d176c14` 画像の再書き出し（WebP 1751KB→788KB、55%減）とサービス画像の帯、小さすぎる文字の底上げ
  - `556fcf5` デザインの体系整理（RGB成分の変数化で手書きrgba26箇所を集約、--gutter で余白を単調化、
    ガター統一、カードのCTA下端揃え、地図カードの高さと枠線、実績数値の書体統一、見出しの改行）
  - `6a3087b` 実績カードの全幅化、電話番号のsticky側への一本化、ヒーローバッジの低彩度化
- 挿絵を6枚作り直した。`移動支援` と `パート` の2枚は、heroの世界観に最も近いため据え置き。
  - `99f2572` `d059e23` 重度訪問介護（居間で同じ時間を過ごす場面）
  - `2eb9222` 居宅介護（台所での家事援助。成人＋手動車いす）
  - `a82c3ed` 行動援護（平坦な住宅街。ヘルパーが車道側）
  - `20cd7c8` 採用ヒーロー（自転車で訪問先へ向かう。唯一の1人画）
  - `1c2b000` 正社員カード新規（居間で一緒に洗濯物をたたむ。約380pxの空洞を解消）

## 検証済み

- 文字サイズ200%での破綻 — **失敗 → 修正済み**。2026-09-11。等倍では崩れず200%でのみ発生することを、390pxの実レイアウトで撮り比べて確認。
  ヘッダーのサイト名がハンバーガーと重なりメニューを開けない、アクセス情報の住所末尾・メールアドレスが欠落、
  下部固定バーが本文26px分を覆う。
- 横方向のオーバーフロー — **失敗 → 修正済み**。2026-09-11。全幅で `scrollWidth = clientWidth + 180`。
  原因は `.hero::after`（`right: -180px`）。`body { overflow-x: hidden }` が症状を隠していた。
- コントラスト比 — **一部失敗 → 修正済み**。2026-09-11。本文約90組はすべて4.5:1以上で問題なし。
  一方でフォーカスリング（対濃緑 1.39:1 / 1.00:1）と入力欄の枠線（1.25:1）が基準3:1を下回っていた。
- 静的チェック — 成功。2026-09-11。`id` 重複なし（22個一意）、`href="#..."` 全件解決、タグの入れ子エラー0件、
  JSON-LD構文正常かつ本文との食い違いなし、`innerHTML`/`eval` 使用0件、秘密情報の混入なし。
- 表記揺れ — 成功。2026-09-11。電話番号・受付時間は全出現箇所で統一。
- 修正後の再検証 — 成功。2026-09-11。
  横方向のはみ出しは `overflow-x: hidden` を外した状態で全幅0を実測。
  文字サイズ200%での破綻4点（ヘッダー、アクセス情報、固定バー、ヒーローカードとバッジ）の解消を実描画で確認。
  等倍表示に劣化がないことも実描画で確認。
- 本番反映後の確認 — 成功。2026-09-11。`https://wakakusa-care.com/` で新しい内容・新しい画像寸法・
  軽量化した画像の配信・`robots.txt`・`sitemap.xml` の応答を確認。
- 挿絵差し替え後の検証 — 成功。2026-09-11。全8枚で `width`/`height` 属性と実寸の一致、
  alt の更新、WebP参照の実在を機械確認。281px・390px・PC全幅・文字200%で実描画確認。
  WebP合計 981KB（画像は1枚増えたが、差し替え前の1751KBより軽い）。
- 未実施: 実機iPhone、実機でのキーボード操作、フォーム実送信。事業所側の最終確認。

## 検証方法の注意

- **スマホ表示の確認に `chrome --headless --window-size=390,...` を使ってはいけない。**
  macOSのChromeはウィンドウ幅に下限（約500px）があり、390pxを指定しても実際は約485〜500pxで組まれる。
  390px幅のiframeに読み込む方式か、CDPのデバイスエミュレーションを使う。
- ただしiframe方式はモバイルブラウザ特有のビューポート挙動を再現しない。
  上記オーバーフローに起因する固定バーの症状はiframeでは出ないため、実機確認が必要。

## 次に行うこと

1. 事業所側に、下記の確認事項リストを送る。ここが止まると残りの多くが進まない。
2. フォントサイズの6段スケールへの集約。**事業所の確認が返ってから着手する。**
   実使用が35種類あり保守しづらいが、実害のあった9.9〜11pxの底上げは済んでいる。
   確認事項が返ると文言が増減してサイズ調整が要るため、先にやると確認作業が2回になる。
   あわせて角丸16種と影12種の集約も同時に行う。
3. 実機iPhoneでの確認。特に、横180pxのはみ出しに起因していた固定バーの症状が解消しているか。

## 判断待ち・禁止事項

- 判断待ち（従来分）: 求人の給与・勤務条件、対応エリア、Formspree完了後の運用。
- 保留: ヒーローカードは文字サイズ200%時にカード内スクロールになる。内容は失われないが、
  写真の下へ流す構成に変える案もある。見た目の判断を伴うため未着手。
- 保留: 1061px未満ではナビ・電話・CTAがヘッダー1段に収まらないため、電話は隠して
  sticky なCTA（`#contact`）に任せている。この帯で電話を常時見せたい場合は、
  ナビ項目を減らすかCTAの文言を短くする必要がある。
- 判断済み: topbar の右枠には、重複していた電話番号の代わりに提供サービス名5つを置いた
  （すべてページ内と meta description に既出の確認済みの事実）。
- 判断済み: 行動援護の挿絵は子どもを描いている。ただし本文に「児童も対象」と書けるかは
  対象年齢の確認が済んでから判断する（下記の確認待ちに残す）。
- 挿絵の作り直し方針: 画風はhero・移動支援・パートを基準にする。枠と同じ比率で生成すれば
  トリミングが出ない（サービス1:1 / 採用ヒーロー1.2:1 / 求人カード3:2）。
  サービスカードは左上に番号バッジ、採用ヒーローは右下にCTAと右上86pxの丸みが重なるため、
  その範囲に人物の顔や主役を置かない。原寸で良く見えても281pxで読めないことがあるので、
  実表示サイズでの確認を必ず行う。
- 判断待ち（レビューで追加、いずれも要事実確認）: 対象となる障がい種別と年齢区分 / 対応エリアの具体的範囲 /
  事業所番号と指定サービスの正式名称 / 苦情受付窓口 / 個人情報の利用目的・保管期間・第三者提供・窓口 /
  返信目安日数と自動返信メールの有無 / 応募・見学の具体的手順 / 相談内容の実例 / 実写を出せるか /
  「移動支援」「日中一時支援」を含めて「5つの障がい福祉サービス」と数えてよいか。
- 禁止事項: 明示承認なしに commit、push、deploy、フォーム送信、送信先や公開ドメインの変更を行わない。
- 禁止事項: 秘密情報をリポジトリやこのファイルへ書かない。`.DS_Store` は自動追加しない。

## 関連資料

- 恒久ルール: `AGENTS.md`
- レビュー詳細（全指摘・優先度・着手順）: `docs/reviews/2026-09-10-site-review.md`
- 設計文書: なし
