# HANDOFF

## 更新情報

- 更新日時: 2026-07-13 Asia/Tokyo
- 更新者: Codex
- 現在の目的: サポートセンターわかくさの利用相談・求人獲得サイトを、標準の引き継ぎ方式で継続運用できる状態にする。

## 現在の状態

- ブランチ: `main`
- HEAD / upstream: 作業開始時に `git rev-parse HEAD` / `git rev-parse origin/main` で一致を確認する。今回も一致確認済み。
- 作業ツリー: `.DS_Store` のみ未コミット。サイトと引き継ぎ文書に未コミット差分はない。
- 現在の変更範囲: `index.html` の全面刷新と、標準の引き継ぎ方式（`AGENTS.md` / `HANDOFF.md`）がリポジトリに反映済み。
- 公開先: Vercel。公開URLは `https://wakakusa-care.com/`、代替URLは `https://wakakusa-care.vercel.app/`。
- 構成: 画像ファイルと単一の `index.html`。`package.json`、ビルドスクリプト、アプリ用テストはない。

## 直近で完了したこと

- 利用者・ご家族・相談支援専門員向けの相談導線と、正社員・パート・ボランティア向けの採用導線を再設計した。
- 既存の事業所・サービス・求人イラストを使い、スマホ用固定CTAとモバイルメニューを追加した。
- `26d447d feat: 利用相談と求人に強いホームページへ全面刷新` を `main` にcommitし、`origin/main`へpushした。
- 独自ドメインの本番HTMLに新しい見出し・フォーム件名が反映され、ページと画像のHTTP 200を確認した。
- このファイルと `AGENTS.md` を追加し、プロジェクト固有の作業ルールを分離した。

## 検証済み

- `git diff --check` — 成功。2026-07-13。
- 内部アンカー、ローカル画像、画像alt、インラインJavaScript、JSON-LDの静的確認 — 成功。2026-07-13。
- `curl`で本番HTMLの新しい見出し・フォーム件名を確認 — 成功。2026-07-13。
- `curl -I`で本番HTMLと `hero.jpg` / `recruit-part.jpg` のHTTP 200を確認 — 成功。2026-07-13。
- 未実施: 実機または実ブラウザでのスマホ・PC視覚回帰確認。フォーム送信も未実施。

## 次に行うこと

1. スマホ幅・PC幅で本番ページを目視し、ヘッダー、CTA、採用カード、フォームの表示を確認する。
2. 事業所側に住所・受付時間・サービス・求人条件・フォーム受信の事実確認を依頼する。
3. 内容変更時は `git diff --check` と静的確認を実行し、明示承認後にcommit・push・Vercel反映を確認する。

## 判断待ち・禁止事項

- 判断待ち: 求人の給与・勤務条件、対応エリア、フォーム返信運用など、現在の掲載内容を事業所側で最終確認すること。
- 禁止事項: ユーザーの明示承認なしにcommit、push、deploy、フォーム送信、公開ドメインや外部フォーム送信先の変更を行わない。
- 禁止事項: 秘密情報をリポジトリやHANDOFFへ記録しない。`.DS_Store`はサイト変更に関係ないため、作業対象へ自動追加しない。

## 関連資料

- 恒久ルール: `AGENTS.md`
- 設計文書: なし
- 詳細な旧引き継ぎ記録: なし（現時点でアーカイブ不要）
